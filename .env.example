# Docker Compose deployment settings. Copy this file to .env and replace the password.
# Never commit .env or real credentials.
APP_ORIGIN=http://localhost:3001
AUTH_DEFAULT_PASSWORD=replace-with-a-unique-password

# Keep false for private HTTP testing. Set true when APP_ORIGIN uses HTTPS.
AUTH_COOKIE_SECURE=false
TRUST_PROXY_HEADERS=false
